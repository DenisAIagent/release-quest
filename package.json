{
  "name": "release-quest",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "serve": "npm run build && npx serve dist -s"
  },
  "devDependencies": {
    "terser": "^5.44.0",
    "vite": "^7.1.7"
  },
  "dependencies": {
    "howler": "^2.2.4",
    "phaser": "^3.90.0"
  }
}
